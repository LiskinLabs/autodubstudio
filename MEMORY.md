# AutoDubStudio - Project Journal & Memory

## 🎯 Текущий Статус (v3.0.0-Beta)
- **Архитектура:** Полный переход на модульную V3. Основная логика в `engine.py`, компоненты в `backend/` (translator, vram_manager, workers).
- **Движок:** Миграция на `Faster-Whisper` завершена. Скорость и VRAM оптимизированы.
- **Философия:** Закреплена в `GEMINI.md`. Приоритет качества (двойной проход перевода, Demucs) над скоростью.
- **Дистрибуция:** Принят план "Portable First". Готовим папку со всеми venv и кэшами.

## ✅ Выполненные задачи
1. [x] Аудит кода и выявление узких мест VRAM.
2. [x] Создание модульной структуры `backend/`.
3. [x] Замена `openai-whisper` на `faster-whisper`.
4. [x] Очистка `engine.py` от избыточных методов.
5. [x] Изоляция воркеров (YouTube DL) от основного UI.
6. [x] Умный пропуск озвучки (`skip_dub`) для оригинального языка (режем прямо из вокала Demucs).
7. [x] Устранение OOM: Перенос микширования (Ducking/Background) из Pydub (в ОЗУ) на потоковый FFmpeg.
8. [x] Исправлен баг сборки MKV (Fragile FFmpeg mapping).
9. [x] Внедрен Time-Stretch (ffmpeg atempo) для подгонки сгенерированного голоса под тайминги субтитров.

## 🚀 Ближайшие планы
- Интеграция нового фронтенда (React/Tauri в папке `gui/`) с нашим бэкендом (FastAPI в `backend/main.py`).
- Окончательное удаление устаревших файлов V2 (например старый app.py).
- Создание скрипта для автоматической сборки Portable-пакета.
